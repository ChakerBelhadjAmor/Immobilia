services:
  immobilia:
    build:
      context: .
      dockerfile: Dockerfile
    image: immobilia-frontend:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
